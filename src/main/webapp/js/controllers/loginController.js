angular.module("cronapp")
    .controller("loginController",
        function ($scope,
                  $window) {
                    
                    
                  
                  });